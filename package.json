{
  "name": "n8n-nodes-google-vertex-embeddings-extended",
  "version": "0.2.0",
  "description": "n8n community sub-node for Google Vertex AI Embeddings with output dimensions support - use with vector stores",
  "keywords": [
    "n8n-community-node-package",
    "google",
    "vertex",
    "embeddings",
    "ai",
    "langchain",
    "vector-store",
    "sub-node"
  ],
  "license": "MIT",
  "homepage": "https://github.com/danblah/n8n-nodes-google-vertex-embeddings-extended",
  "author": {
    "name": "Dan Blah",
    "email": "dan@blahs.life"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/danblah/n8n-nodes-google-vertex-embeddings-extended.git"
  },
  "main": "index.js",
  "scripts": {
    "build": "tsc && mkdir -p dist/nodes/EmbeddingsGoogleVertexExtended && mkdir -p dist/credentials && cp -r nodes/EmbeddingsGoogleVertexExtended/*.svg dist/nodes/EmbeddingsGoogleVertexExtended/ 2>/dev/null || true",
    "dev": "tsc --watch",
    "format": "prettier nodes credentials --write",
    "lint": "eslint nodes credentials --ext .ts",
    "lintfix": "eslint nodes credentials --ext .ts --fix",
    "prepublishOnly": "npm run build && npm run lint -c .eslintrc.prepublish.json nodes credentials"
  },
  "files": [
    "dist"
  ],
  "n8n": {
    "n8nNodesApiVersion": 1,
    "credentials": [
      "dist/credentials/GoogleVertexAuth.credentials.js"
    ],
    "nodes": [
      "dist/nodes/EmbeddingsGoogleVertexExtended/EmbeddingsGoogleVertexExtended.node.js"
    ]
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.11.0",
    "@typescript-eslint/parser": "^6.19.0",
    "eslint": "^8.56.0",
    "eslint-plugin-n8n-nodes-base": "^1.16.1",
    "n8n-workflow": "^1.25.1",
    "prettier": "^3.2.4",
    "typescript": "^5.3.3"
  },
  "dependencies": {
    "@langchain/google-vertexai": "^0.0.21",
    "google-auth-library": "^9.6.3"
  },
  "peerDependencies": {
    "n8n-workflow": "^1.25.1"
  }
}
